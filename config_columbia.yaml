grid:
  type: hexsim

hexsim:
  workspace: "Columbia [small]"
  species: chinook
  temperature_csv: River Temperature.csv

estuary:
  salinity_cost:
    # Columbia: freshwater scenario; zero slopes disable osmoregulation cost
    # (cost = 1.0 for all salinities). Migrated from old S_tol: 999, k: 0.0
    # disable pattern.
    salinity_hyper_cost: 0.0
    salinity_hypo_cost: 0.0
  do_avoidance:
    lethal: 0.0
    high: 0.0
  seiche_pause:
    dSSHdt_thresh_m_per_15min: 999
